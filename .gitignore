### Node
node_modules/
*.log
npm-debug.log*

### Build outputs
dist/
.next/
coverage/

### Environment — never commit secrets
.env
.env.local
.env.production.local
.env.uat

### OS
.DS_Store
Thumbs.db

### Backend
uploads/
# NOTE: prisma/migrations/ MUST be tracked — required for `prisma migrate deploy` in CI/prod.

### Frontend
frontend/tsconfig.tsbuildinfo

### Infrastructure doc (contains secrets — local only)
docs/INFRASTRUCTURE.md

### Mobile / Expo
mobile/.expo/
mobile/ios/
mobile/android/
mobile/dist/
mobile/
.claude/
frontend/.claude/

### Stale HTML previews (don't commit)
docs/*-preview.html
docs/.Rhistory
